<p><%= t('posts.count_favorite') %>:<br><%= @favorite_count %></p>
<% if @post.user.id != current_user.id %>
  <% if @favorite.present? %>
    <p><%= link_to t('posts.delete_favorite'), favorite_path(id: @favorite.id), method: :delete,class:"btn btn-red" %></p>
  <% else %>
    <p><%= link_to t('posts.do_favorite'), favorites_path(post_id: @post.id), method: :post,class:"btn btn-pink" %></p>
  <% end %>
<% end %>
