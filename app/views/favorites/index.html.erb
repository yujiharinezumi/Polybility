<h1>お気に入り一覧</h1>

<% @favorites.each do |f| %>

    <p>ユーザー名：<%= f.user.name %></p>
    <p><%= image_tag f.post.image.url %></P
    <p><%= f.post.content%></p>
  <% end %>

  <%= link_to "投稿一覧", posts_path%>
