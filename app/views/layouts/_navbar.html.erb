<nav class="navbar navbar-expand-lg navbar-dark bg-info">
  <a class="navbar-brand" href="#">Polybility</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item nav-link ">
          <%= link_to "ユーザー一覧", users_path,class:"white" %>
        </li>
        <li class="nav-item nav-link">
           <%= link_to "投稿一覧", posts_path,class:"white" %>
        </li>
        <li class="nav-item nav-link ">
          <%= link_to "お気に入り一覧", favorites_path,class:"white" %>
        </li>
      <% else %>
        <li class="nav-item nav-link ">
          <%= link_to "新規登録", new_user_registration_path,class:"white" %>
        </li>
        <li class="nav-item nav-link ">
          <%= link_to "ログイン", new_user_session_path,class:"white" %>
        </li>
      <% end %>
    </ul>

    <form class="form-inline my-2 my-lg-0">
      <ul class="navbar-nav mr-auto">
        <% if current_user.try(:admin?) %>
          <li class="nav-item nav-link white">管理者</li>
        <% end %>
        <% if user_signed_in? %>
          <li class="nav-item nav-link nav-right ">
             <%=link_to image_tag(current_user.icon.url,class:"nav-icon"),user_path(current_user.id) %>
          </li>
          <li class="nav-item nav-link ">
            <%= link_to "#{current_user.name}さん", user_path(current_user.id),class:"white" %>
          </li>
          <li class="nav-item nav-link">
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete,class:"white" %>
          </li>
        <% else %>
          <li class="nav-item nav-link navbar-right ">
            <%= link_to "ゲストユーザーでログイン", users_guest_sign_in_path, method: :post,class:"white" %>
          </li>
        <% end %>
      </ul>
    </form>
  </div>
</nav>
